name: Multi-ATS MBA Internship Scraper

on:
  schedule:
    # Every 6 hours (UTC)
    - cron: "0 */6 * * *"
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Dry run (no email, no state commit)"
        required: false
        default: "false"
        type: choice
        options:
          - "false"
          - "true"
      test_mode:
        description: "Run test subset only"
        required: false
        default: "false"
        type: choice
        options:
          - "false"
          - "true"

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: requirements.txt

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run unit tests
        run: python -m pytest tests/ -q

      - name: Run scraper test
        run: python job_scraper_orchestrator.py --test --dry-run --skip-email --verbose

      - name: Run scraper
        env:
          SMTP_HOST: ${{ secrets.SMTP_HOST }}
          SMTP_PORT: ${{ secrets.SMTP_PORT }}
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
          ALERT_EMAIL: ${{ secrets.ALERT_EMAIL }}
        run: |
          if [ "${{ github.event.inputs.test_mode || 'false' }}" = "true" ]; then
            python job_scraper_orchestrator.py --test --verbose --skip-email
          elif [ "${{ github.event.inputs.dry_run || 'false' }}" = "true" ]; then
            python job_scraper_orchestrator.py --dry-run --verbose
          else
            python job_scraper_orchestrator.py --verbose
          fi

      - name: Commit previous jobs state
        if: github.event.inputs.dry_run != 'true' && github.event.inputs.test_mode != 'true'
        run: |
          git config user.name "mba-internship-scraper[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add previous_jobs.json data/latest_results.json logs/
          if git diff --staged --quiet; then
            echo "No state changes to commit"
          else
            git commit -m "chore(scraper): update job state [skip ci]"
            git push
          fi
